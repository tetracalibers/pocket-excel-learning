---
import { type FnReturn } from "../../content/config"
import { Table, TableBody, TableBodyCell, TableBodyRow, Badge } from "flowbite-svelte"

interface Props {
  patterns: FnReturn[]
}

const { patterns } = Astro.props
---

<Table>
  <TableBody>
    {
      patterns.map((pattern) => (
        <TableBodyRow class="border-none">
          <TableBodyCell class="text-inherit py-2">{pattern.if}</TableBodyCell>
          <TableBodyCell class="text-inherit py-2">{pattern.summary}</TableBodyCell>
        </TableBodyRow>
      ))
    }
  </TableBody>
</Table>
